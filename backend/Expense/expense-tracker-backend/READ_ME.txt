Create a .env file in the same folder as server.js and fill the following credentials for email verification enabling and also 

EMAIL_USER
EMAIL_PASSWORD
JWT_SECRET
PORT

Use email password from your google key passwords not original 

Generate your JWT key 

and set port accordingly the frontend too

There is also a Schema.sql file to connect the database from the backend...
run the whole file in your Workbench

Install all dependencies of both LSTM RNN Model, Node.js and React.js both frontend and backend

Then node server.js to start backend server... in the directory where server.js is there

and use npm install in the frontend folder 
and npm start to run the frontendd